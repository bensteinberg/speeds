[tool.poetry]
name = "speeds"
version = "0.2.5"
description = "A small program for graphing cable modem speeds"
authors = ["Ben Steinberg <bsteinberg@law.harvard.edu>"]
license = "GPL-3.0-only"

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.1.3"
jsonlines = "^2.0.0"
python-dotenv = "^1.2.2"

[tool.poetry.dev-dependencies]
pytest = "^9.0.3"
flake8 = "^3.9.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
